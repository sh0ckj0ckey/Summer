<Page
    x:Class="Summer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Summer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls" 
    xmlns:muxca="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">

    <Page.Resources>
        <ThemeShadow x:Name="CommonShadow"/>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="48"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>

        <Grid x:Name="ShadowReceiverGrid" Grid.ColumnSpan="3" Grid.RowSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <Grid x:Name="CanvasGrid" Grid.Column="1" Grid.Row="1" CornerRadius="4" Translation="0,0,32" Shadow="{ThemeResource CommonShadow}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.Background>
                <ImageBrush ImageSource="{ThemeResource SketchCanvasTextureImage}" Stretch="UniformToFill" Opacity="0.3"/>
            </Grid.Background>
            <ScrollViewer x:Name="SketchCanvasScrollViewer" ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="5" Background="Transparent"
                          HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <Grid x:Name="SketchCanvasGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image x:Name="SketchCanvasBackgroundImage" Opacity="0.1" Source="{x:Null}" Visibility="Collapsed" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    <InkCanvas x:Name="SketchCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </Grid>
            </ScrollViewer>
        </Grid>

        <Grid x:Name="TitleBarGrid" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Canvas.ZIndex="1" Height="48" HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="Transparent"/>

        <Grid x:Name="ToolBarGrid" Grid.Column="1" Grid.Row="0" Canvas.ZIndex="1" Margin="8,0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Margin="3" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <InkToolbar x:Name="SketchToolbar" TargetInkCanvas="{x:Bind SketchCanvas}" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <InkToolbarBallpointPenButton x:Name="BallpointPenInkToolbarButton"/>
                <InkToolbarPencilButton x:Name="PencilInkToolbarButton"/>
                <InkToolbarHighlighterButton x:Name="HighlighterInkToolbarButton"/>
                <InkToolbarEraserButton x:Name="EraserInkToolbarButton"/>
                <InkToolbarStencilButton x:Name="StencilInkToolbarButton"/>
            </InkToolbar>
        </Grid>

        <Grid x:Name="StatusBarGrid" Grid.Column="1" Grid.Row="2" Padding="8,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel x:Name="StatusBarStackPanel" Spacing="8" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="ZoomOutButton" x:Uid="ZoomOutButton" Content="&#xE71F;" Padding="0" Height="38" Width="38" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="ZoomOutButton_Click"/>
                <Button x:Name="ZoomInButton" x:Uid="ZoomInButton" Content="&#xE12E;" Padding="0" Height="38" Width="38" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="ZoomInButton_Click"/>
                <Border Width="1" Height="26" Opacity="0.1" Background="{ThemeResource ApplicationForegroundThemeBrush}"/>
                <Button x:Name="ZoomResetButton" x:Uid="ZoomResetButton" FontSize="14" Padding="0" Height="38" Width="64" FontFamily="Segoe Script" Background="Transparent" BorderBrush="Transparent" VerticalAlignment="Center" Click="ZoomResetButton_Click">
                    <TextBlock>
                        <Run x:Name="CanvasZoomFactorTextBlock" Text="100"/>
                        <Run Text="%"/>
                    </TextBlock>
                </Button>
            </StackPanel>

            <StackPanel x:Name="LogoStackPanel" Opacity="1" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right" VerticalAlignment="Center" IsHitTestVisible="False">
                <Image HorizontalAlignment="Left" VerticalAlignment="Center" Width="36" Height="36">
                    <Image.Source>
                        <BitmapImage UriSource="Assets/Logo/summer_logo.png" DecodePixelHeight="72"/>
                    </Image.Source>
                </Image>
                <TextBlock Text="Summer" Margin="0,1,0,-1" FontFamily="Segoe Script" FontSize="18" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="FeatureBarGrid" Grid.Column="2" Grid.Row="1" Padding="0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel Spacing="8" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                <ToggleButton x:Name="DrawWithHandToggleButton" x:Uid="DrawWithHandButton" Content="&#xED5F;" Padding="0" Width="38" Height="38" IsChecked="False" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Checked="DrawWithHandToggleButton_Checked" Unchecked="DrawWithHandToggleButton_Unchecked"/>
                <ToggleButton x:Name="ShapeRecognizeToggleButton" x:Uid="ShapeRecognizeButton" Content="&#xF157;" Padding="0" Width="38" Height="38" IsChecked="False" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Checked="ShapeRecognizeToggleButton_Checked" Unchecked="ShapeRecognizeToggleButton_Unchecked"/>
                <Border Width="26" Height="1" Opacity="0.1" Background="{ThemeResource ApplicationForegroundThemeBrush}"/>
                <ToggleButton x:Name="PictureToggleButton" x:Uid="PictureBackgroundButton" Content="&#xE91B;" Padding="0" Width="38" Height="38" IsChecked="False" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Checked="PictureToggleButton_Checked" Unchecked="PictureToggleButton_Unchecked"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="ControlBarGrid" Grid.Column="0" Grid.Row="1" Padding="0,8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel Spacing="8" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                <ToggleButton x:Name="FullscreenButton" x:Uid="FullscreenButton" Padding="0" Height="38" Width="38" HorizontalAlignment="Center" Checked="FullscreenButton_Checked" Unchecked="FullscreenButton_Unchecked">
                    <ToggleButton.Content>
                        <FontIcon Glyph="&#xE1D9;" FontSize="14" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </ToggleButton.Content>
                </ToggleButton>
                <ToggleButton x:Name="TopmostButton" x:Uid="TopmostButton" Padding="0" Height="38" Width="38" HorizontalAlignment="Center" Checked="TopmostButton_Checked" Unchecked="TopmostButton_Unchecked">
                    <ToggleButton.Content>
                        <FontIcon Glyph="&#xE141;" FontSize="14" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </ToggleButton.Content>
                </ToggleButton>
            </StackPanel>

            <StackPanel Spacing="8" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <Button x:Name="SaveButton" x:Uid="SaveButton" Content="&#xE105;" Padding="0" Width="38" Height="38" FontSize="16" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="SaveButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border Width="26" Height="1" Opacity="0.1" Background="{ThemeResource ApplicationForegroundThemeBrush}"/>
                <Button x:Name="SettingsButton" x:Uid="SettingsButton" Padding="0" Height="38" Width="38" Click="SettingsButton_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <Button.Content>
                        <muxc:AnimatedIcon Height="16" Width="16">
                            <muxc:AnimatedIcon.Source>
                                <muxca:AnimatedSettingsVisualSource/>
                            </muxc:AnimatedIcon.Source>
                            <muxc:AnimatedIcon.FallbackIconSource>
                                <muxc:SymbolIconSource Symbol="Setting"/>
                            </muxc:AnimatedIcon.FallbackIconSource>
                        </muxc:AnimatedIcon>
                    </Button.Content>
                </Button>
            </StackPanel>

            <muxc:TeachingTip x:Name="SettingsTeachingTip" Target="{x:Bind SettingsButton}" IsOpen="False" IsLightDismissEnabled="True">
                <Grid Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Row="1" Grid.ColumnSpan="3" Height="1" Opacity="0.1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,4,0,4" Background="{ThemeResource SettingsHorizontalSeparatorLineBackground}"/>
                    <Border Grid.Row="3" Grid.ColumnSpan="3" Height="1" Opacity="0.1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,4,0,4" Background="{ThemeResource SettingsHorizontalSeparatorLineBackground}"/>
                    <Border Grid.Row="5" Grid.ColumnSpan="3" Height="1" Opacity="0.1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,4,0,4" Background="{ThemeResource SettingsHorizontalSeparatorLineBackground}"/>
                    <Border Grid.Row="7" Grid.ColumnSpan="3" Height="1" Opacity="0.1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,4,0,4" Background="{ThemeResource SettingsHorizontalSeparatorLineBackground}"/>
                    <Border Grid.Column="1" Grid.RowSpan="8" Width="1" Opacity="0.1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Margin="4,0,4,0" Background="{ThemeResource SettingsVerticalSeparatorLineBackground}"/>

                    <FontIcon Grid.Column="0" Grid.Row="0" Glyph="&#xE9E9;" FontSize="20" FontFamily="{ThemeResource SymbolThemeFontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Button x:Name="CloseSettingsButton" Grid.Column="2" Grid.Row="0" Content="&#xEDAE;" Margin="0" Padding="6" BorderBrush="Transparent" Background="Transparent" FontSize="14" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="CloseSettingsButton_Click" HorizontalAlignment="Right" VerticalAlignment="Center"/>

                    <FontIcon Grid.Column="0" Grid.Row="2" Glyph="&#xE771;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <muxc:RadioButtons Grid.Column="2" Grid.Row="2" MaxColumns="3" SelectedIndex="{x:Bind AppSettings.Appearance,Mode=TwoWay}" Margin="16,0,0,0">
                        <RadioButton MinWidth="0" Margin="0,0,8,0" Content="&#xE793;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                        <RadioButton MinWidth="0" Margin="8,0,0,0" Content="&#xE794;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </muxc:RadioButtons>

                    <FontIcon Grid.Column="0" Grid.Row="4" Glyph="&#xE776;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <muxc:RadioButtons Grid.Column="2" Grid.Row="4" MaxColumns="3" SelectedIndex="{x:Bind AppSettings.HandednessMode,Mode=TwoWay}" Margin="16,0,0,0">
                        <RadioButton MinWidth="0" Margin="0,0,8,0" Content="&#xEA37;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="RadioButton_Click"/>
                        <RadioButton MinWidth="0" Margin="8,0,0,0" Content="&#xEA55;" FontSize="18" FontFamily="{ThemeResource SymbolThemeFontFamily}" Click="RadioButton_Click"/>
                    </muxc:RadioButtons>

                    <Image Grid.Column="0" Grid.Row="6" Width="42" Height="42" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image.Source>
                            <BitmapImage UriSource="Assets/Logo/summer_logo_detailed.png" DecodePixelHeight="84"/>
                        </Image.Source>
                    </Image>
                    <StackPanel Grid.Column="2" Grid.Row="6" Margin="12,0,0,0" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Margin="4,0,0,-6" Text="Summer" Opacity="0.8" FontSize="16" FontFamily="Segoe Script" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Margin="4,0,0,0" Text="{x:Bind AppVersion}" Opacity="0.6" FontSize="12" FontFamily="Segoe Script" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Grid.Row="8" Grid.ColumnSpan="3" Margin="4,8,0,0" Opacity="0.5" Text="©2026 NoMewing" FontSize="12" FontFamily="Segoe Script" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                </Grid>
            </muxc:TeachingTip>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="NormalState">
                    <VisualState.Setters>
                        <Setter Target="FullscreenButton.Visibility" Value="Visible"/>
                        <Setter Target="TopmostButton.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="FullScreenState">
                    <VisualState.Setters>
                        <Setter Target="FullscreenButton.Visibility" Value="Visible"/>
                        <Setter Target="TopmostButton.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="PiPState">
                    <VisualState.Setters>
                        <Setter Target="FullscreenButton.Visibility" Value="Collapsed"/>
                        <Setter Target="TopmostButton.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup>
                <VisualState x:Name="RightHandState">
                    <VisualState.Setters>
                        <Setter Target="FeatureBarGrid.Column" Value="2"/>
                        <Setter Target="ControlBarGrid.Column" Value="0"/>
                        <Setter Target="StatusBarStackPanel.HorizontalAlignment" Value="Left"/>
                        <Setter Target="LogoStackPanel.HorizontalAlignment" Value="Right"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="LeftHandState">
                    <VisualState.Setters>
                        <Setter Target="FeatureBarGrid.Column" Value="0"/>
                        <Setter Target="ControlBarGrid.Column" Value="2"/>
                        <Setter Target="StatusBarStackPanel.HorizontalAlignment" Value="Right"/>
                        <Setter Target="LogoStackPanel.HorizontalAlignment" Value="Left"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="560"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ToolBarGrid.HorizontalAlignment" Value="Center"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ToolBarGrid.HorizontalAlignment" Value="Left"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
