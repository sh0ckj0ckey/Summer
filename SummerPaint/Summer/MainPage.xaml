<Page
    x:Class="Summer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Summer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d" RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    x:Name="SummerMainPage" Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="InkToolbarStyle1" TargetType="InkToolbar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="InkToolbar">
                        <StackPanel x:Name="Panel" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Orientation="Horizontal"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <InkCanvas x:Name="MainCanvas" Grid.Row="1" Grid.RowSpan="2"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <InkToolbar Style="{StaticResource InkToolbarStyle1}" Grid.Row="1" HorizontalAlignment="Center" Background="White" TargetInkCanvas="{x:Bind MainCanvas}"/>
        </StackPanel>
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LemonChiffon">
            <TextBlock Text="夏花 Summer" Foreground="Sienna" VerticalAlignment="Center" Margin="12,0,0,0" FontSize="12"/>
        </Grid>
        <Image x:Name="BackgroundImage" Grid.Row="1" Grid.RowSpan="2" Stretch="UniformToFill" Opacity="0.3" IsHitTestVisible="False"/>
    </Grid>
</Page>
